import { Client } from 'src/app/client/model/client';

export class Account {
  id: number;
  numCompte: string;
  typeCompte: string;
  solde: number;
  createAt:Date;
  idClient:number;
  nomClient:string;
  
}
